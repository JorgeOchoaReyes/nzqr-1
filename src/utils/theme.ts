import { createTheme } from "@mui/material"; 

export const theme = createTheme({
  palette: { 
    primary: {
      main: "#1eb854",
      100: "#acb4e2", 
      contrastText: "#ffffff",
    }, 
    secondary: {
      main: "#000000",
      contrastText: "#ffffff",
    },
  },
});
